# mamba env create --file=env.yaml
name: synapse
channels:
    - pytorch
    - nvidia
    - conda-forge
dependencies:
    - imagecodecs
    - magicgui
    - napari
    - pip
    - pooch
    - pyqt
    - python-xxhash
    - python-elf >=0.4.8
    - pytorch
    - pytorch-cuda>=11.8  # you may need to update the cuda version to match your system
    - segment-anything
    - torchvision
    - torch_em >=0.7.0
    - tqdm
    - timm
    - pip:
        - git+https://github.com/ChaoningZhang/MobileSAM.git
